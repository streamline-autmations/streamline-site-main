/**
 * MagneticButton — the canonical shared magnetic-pull wrapper for primary CTAs.
 * Re-exported under the spec name; `MagneticCTA` is the implementation
 * (subtle spring pull toward the cursor, auto-disabled on touch + reduced-motion).
 */
export { default } from './MagneticCTA';
