import PagePlaceholder from '../../components/PagePlaceholder';

export default function RecklessBear() {
  return (
    <PagePlaceholder
      eyebrow="Case study · Site + CRM + AI"
      title={
        <>
          RecklessBear <span className="font-serif italic text-site-accent">Apparel</span>.
        </>
      }
      blurb="Website + admin + CRM + 12-stage production tracking + WhatsApp automation + an AI quote engine. Active retainer."
      phase="Phase 5"
    />
  );
}
